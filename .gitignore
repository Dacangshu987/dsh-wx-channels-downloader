node_modules/
*.log
.DS_Store
Thumbs.db

# local runtime data never belongs in the repo
certs/
downloads/
*.db
*.db-shm
*.db-wal

# scratch / ad-hoc
test/tmp/

# Go sidecar: build artifact + synced assets are generated (source lives in sidecar/*.go)
sidecar/wxchannels-inject.exe
sidecar/assets/
sidecar/*.exe~
